# @vis-talk/vega-builder

Convert natural language input to vega-lite chart spec.

## Building

Run `nx build vega-builder` to build the library.

## Running unit tests

Run `nx test vega-builder` to execute the unit tests via [Jest](https://jestjs.io).
